export interface theme{
    dark: boolean,
    light: boolean,
    cyan: boolean
};

export interface operation {
    total: null | string,
    siguiente: null | string,
    operador: null | string
}
